# Flat node_modules so electron-builder bundles all (transitive) dependencies.
# Without this, pnpm's symlinked layout makes electron-builder miss deps like
# electron-store's "conf", crashing the packaged app.
node-linker=hoisted
